function slasher(arr, howMany){ // the function takes 2 parameters: the array and the number of elements to be removed from its head
  	return arr.slice(howMany); // returns the array starting from index[howMany], so the array from index 0 to howMany is removed
  } 

slasher(); // enter the array here
